# API Base URL
# For development with docker-compose, use http://localhost:8080 (direct backend access)
# For production with nginx reverse proxy, use /api (relative URL)
NEXT_PUBLIC_API_URL=http://localhost:8080

# Environment
NODE_ENV=development