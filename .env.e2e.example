# E2E Test Environment Configuration
# This file contains environment variables for E2E testing.
# Copy this file to .env.e2e to use it for E2E tests.

# Project Name for E2E tests
COMPOSE_PROJECT_NAME=sandbox-e2e

# Port Offset for E2E tests (1 = ports 3001, 8081, 5433, etc.)
PORT_OFFSET=1

# Calculated Port Numbers
FRONTEND_PORT=3001
BACKEND_PORT=8081
POSTGRES_PORT=5433
DEBUG_PORT=5006
NGINX_PORT=81

# Database Configuration
POSTGRES_DB=sandbox
POSTGRES_USER=sandbox
POSTGRES_PASSWORD=sandbox

# Use tmpfs for E2E tests (faster, no persistent data needed)
DB_PERSISTENCE_MODE=tmpfs
